{
  "name": "TS",
  "version": "0.0.1",
  "description": "1. install node 2. npm i typescript -g -> tsc -v 3. tsc -v 4. tsc --init 初始化配置文件 5. tsc -w(watch and compile) -> node index.js   - npm i ts-node -g 6. tsconfig.json -> strict",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "jack <mengbinhao2018@gmail.com> (http://github.com/mengbinhao)",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.14.2"
  }
}
